@* a common component to be used for any error message logging, so far this is Home.razor & Utilities.razor *@
@* use a prop to pass error message into this component *@

@* 

<Panel Title="❌ Error" BorderColor="@Color.Red">
  <Markup Content="@errorMessage" Foreground="@Color.Red" />
</Panel>

@code {
    [Parameter]
    public string ErrorMessage { get; set; }
}

 *@